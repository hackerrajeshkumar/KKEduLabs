"""Agent layer: the answerer (with its tools) and the grounding verifier.

Import the wired agents from ``kkedu_rag.agents.wiring`` (re-exports AGENT and
VERIFIER). Kept out of this init so the package name never shadows the installed
``agents`` SDK during its own submodule imports.
"""
